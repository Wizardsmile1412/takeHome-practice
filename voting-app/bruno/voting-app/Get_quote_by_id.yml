info:
  name: Get_quote_by_id
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/quotes/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
